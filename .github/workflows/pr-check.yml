name: PR Check

on:
  pull_request:
    branches:
      - main

jobs:
  docker-test:
    name: Build & Test Image
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗️ Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.1.18

      - name: 📦 Install dependencies
        run: bun install

      - name: 🧼 Lint project files
        run: bun run lint

      - name: 🧼 Lint Dockerfile
        run: ./scripts/lint-image.sh

      - name: 🔐 Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐳 Build Docker Image with GHCR cache
        uses: docker/build-push-action@v6
        with:
          context: ./docker
          file: ./docker/Dockerfile.cpu
          push: false
          tags: ghcr.io/voxextract-labs/vox-whisper:ci-cache
          cache-from: type=registry,ref=ghcr.io/voxextract-labs/vox-whisper:ci-cache
          cache-to: type=registry,ref=ghcr.io/voxextract-labs/vox-whisper:ci-cache,mode=max,compress=true

      - name: 🧪 Run Test Image
        run: ./scripts/test-image.sh
